[project]
name = "weather-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "prabhu", email = "prabhukadode1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "google-genai>=2.16.0",
    "httpx>=0.28.1",
    "ollama>=0.6.2",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
weather-agent = "weather_agent:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "ruff>=0.16.4",
]
